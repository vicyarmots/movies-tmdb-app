import { MovieGridSkeleton } from "@/entities/movies/ui/skeleton/movies-skeleton";

const UserWatchlistLoading = () => {
  return <MovieGridSkeleton viewMode={"grid"} />;
};

export default UserWatchlistLoading;
